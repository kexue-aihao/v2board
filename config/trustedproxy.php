<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 可信反向代理
    |--------------------------------------------------------------------------
    |
    | 站点始终经 nginx 反代到 127.0.0.1:6600。不声明可信代理时 Symfony 不会解析
    | 任何转发头，REMOTE_ADDR 恒为回环地址，$request->ip() 以及订阅审计、注册
    | 频率限制、会话记录都拿不到真实来源。
    |
    | 默认只信任回环地址。Symfony 从 X-Forwarded-For 链的右端向左取第一个不属于
    | 可信代理的地址，而 nginx 的 $proxy_add_x_forwarded_for 会把真实对端追加在
    | 链尾，因此客户端自行伪造的前缀条目不会被采纳。
    |
    | 站点前面还有 CDN（例如 Cloudflare）时，必须把 CDN 的回源地址段一并加入，
    | 否则取到的会是 CDN 边缘节点地址而不是访客地址。多个值用英文逗号分隔，
    | 支持 CIDR 写法。设为 * 表示信任所有对端，仅在确认最外层代理会覆盖而不是
    | 追加 X-Forwarded-For 时才可使用，否则任何客户端都能伪造来源地址。
    |
    */

    'proxies' => env('TRUSTED_PROXIES', '127.0.0.1,::1'),

];
